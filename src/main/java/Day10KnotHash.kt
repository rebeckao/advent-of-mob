class Day10KnotHash {
    fun calculateKnotHash(twistLengths: IntArray, elementListLength: Int) : Int {
        val knot = (0 until elementListLength).toList().toIntArray()
        var currentPosition = 0
        var skipSize = 0

        for ((skipSize, twistLength) in twistLengths.withIndex()) {

        }


        return 0
    }


}